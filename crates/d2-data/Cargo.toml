[package]
name = "d2-data"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Diablo II game rules, read at run time from the operator's own install"

[dependencies]
d2-formats = { path = "../d2-formats" }

[lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"
