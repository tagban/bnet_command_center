[package]
name = "d2-drlg"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Diablo II level generation: the world a game seed produces, as the 1.14d engine builds it"

[dependencies]
d2-data = { path = "../d2-data" }

[dev-dependencies]
d2-formats = { path = "../d2-formats" }

[lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"
